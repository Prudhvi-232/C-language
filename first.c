#include <stdio.h>  

int main(){
    int a, b;  // declare two integer variables
    printf("Enter the first number: ");
    scanf("%d", &a);     // read the value of 'a' from user input and store it in variable 'a'
    
}